{
  "name": "hoyer",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "generate": "NODE_ENV=prod nuxt generate",
    "start": "nuxt start",
    "deploy": "push-dir --dir=dist --branch=pages --cleanup --remote=github --allow-unclean"
  },
  "dependencies": {
    "axios": "^0.20.0",
    "nuxt": "^2.14.1",
    "vuelidate": "^0.7.5"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.11.2",
    "push-dir": "^0.4.1"
  }
}